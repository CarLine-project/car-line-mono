# Компонент завантаження чеків (Receipt Upload)

## Опис

Компонент для завантаження та попередньої обробки фото чеків у форматі зображень. Дозволяє користувачам фотографувати чеки через камеру або вибирати їх з галереї.

## Функціональність

### Основні можливості:

- 📸 Фотографування чеків через камеру пристрою
- 🖼️ Вибір зображень з галереї
- 👀 Попередній перегляд завантажених фото
- 🔍 **Перегляд фото у повному розмірі** (клік на фото)
- 🗑️ Видалення небажаних фото
- 📊 Відображення інформації про файли (розмір, назва)
- ⚙️ **Автоматична обробка та валідація фото**
- ✅ Індикатори стану: обробка, успіх, помилка
- ⚠️ **Детальні повідомлення про помилки валідації**
- 🚫 Блокування відправки форми з помилковими фото
- 🔢 Обмеження кількості фото (до 5 за замовчуванням)

## Використання

### Імпорт компонента

```typescript
import { ReceiptUpload, ReceiptPhoto } from "../../shared/ui";
```

### Базовий приклад

```typescript
import { useState } from "react";
import { ReceiptUpload, ReceiptPhoto } from "../../shared/ui";

const MyForm = () => {
  const [photos, setPhotos] = useState<ReceiptPhoto[]>([]);

  const handlePhotosChange = (newPhotos: ReceiptPhoto[]) => {
    setPhotos(newPhotos);
    console.log("Завантажено фото:", newPhotos);
  };

  return (
    <form>
      {/* Інші поля форми */}

      <ReceiptUpload
        maxPhotos={5}
        onPhotosChange={handlePhotosChange}
        disabled={false}
      />

      {/* Кнопка відправки форми */}
    </form>
  );
};
```

### Props

| Prop             | Тип                                | За замовчуванням | Опис                           |
| ---------------- | ---------------------------------- | ---------------- | ------------------------------ |
| `maxPhotos`      | `number`                           | `5`              | Максимальна кількість фото     |
| `onPhotosChange` | `(photos: ReceiptPhoto[]) => void` | -                | Callback при зміні списку фото |
| `disabled`       | `boolean`                          | `false`          | Чи відключений компонент       |

### Типи

```typescript
type ReceiptPhotoStatus = "processing" | "success" | "error";

interface ReceiptPhoto {
  id: string; // Унікальний ID фото
  dataUrl: string; // Base64 data URL зображення
  fileName: string; // Назва файлу
  size: number; // Розмір в байтах
  uploadedAt: Date; // Дата завантаження
  status: ReceiptPhotoStatus; // Статус обробки
  errorMessage?: string; // Повідомлення про помилку (якщо status === 'error')
}
```

## Інтеграція в існуючі форми

Компонент вже інтегровано у:

### 1. Форма витрат (ExpenseForm)

**Файл:** `src/features/expenses/ui/expense-form.tsx`

Використовується при додаванні витрат до автомобіля. Дозволяє прикріпити фото чеків до витрати.

### 2. Форма технічного обслуговування (MaintenanceForm)

**Файл:** `src/features/maintenance/ui/maintenance-form.tsx`

Використовується при додаванні запису про ТО. Дозволяє прикріпити фото рахунків за обслуговування.

## Демо-сторінка

Для тестування функціоналу доступна окрема демо-сторінка:

**URL:** `/demo/receipt-upload`

**Файл:** `src/pages/demo/receipt-upload-demo.tsx`

На цій сторінці можна:

- Протестувати завантаження фото
- Перевірити роботу камери
- Подивитися інформацію про завантажені файли
- Симулювати обробку чеків

### Доступ до демо:

1. Запустіть додаток
2. Авторизуйтесь
3. Перейдіть за URL: `/demo/receipt-upload`

## Захист від помилок

### Блокування відправки форми

Форма **не дозволить зберегти дані**, якщо:

1. ⏳ Є фото у стані `processing` (показує: "Зачекайте, фото чеків ще обробляються...")
2. ❌ Є фото у стані `error` (показує: "Виправте помилки у N фото перед збереженням")

Користувач повинен:

- Зачекати завершення обробки всіх фото
- Видалити фото з помилками
- Завантажити нові фото замість помилкових

## Технічні деталі

### Підтримувані формати

- JPEG/JPG
- PNG
- WebP
- GIF

### Обробка зображень

Компонент конвертує завантажені файли у формат Base64 Data URL для:

- Швидкого попереднього перегляду
- Легкої передачі на бекенд (у майбутньому)
- Зберігання в state без додаткових залежностей

### Стани компонента

1. **Порожній стан** - показує заглушку з інструкціями
2. **Завантаження** - показує спіннер під час вибору файлів
3. **Заповнений** - показує сітку з превью фото

### Стани кожного фото

1. **processing** (⏳ Жовтий) - Фото обробляється та валідується
2. **success** (✅ Зелений) - Фото успішно оброблено, готове до відправки
3. **error** (❌ Червоний) - Помилка валідації, потрібно видалити та завантажити інше фото

### Валідація фото

При завантаженні кожне фото автоматично проходить симуляцію валідації (1-3 секунди). Можливі помилки:

- "Фото занадто розмите. Спробуйте зробити чіткіше знімок"
- "Не вдалося розпізнати чек. Переконайтесь, що весь чек видно на фото"
- "Погана якість зображення. Використайте краще освітлення"
- "Чек занадто темний. Зробіть фото при кращому освітленні"
- "Не вдалося знайти суму на чеку. Перевірте, чи весь чек на фото"
- "Формат зображення не підтримується для розпізнавання"

**Шанс помилки: ~30%** (налаштовується в коді)

## Майбутня інтеграція з бекендом

Наразі компонент працює як заглушка. Для повної інтеграції потрібно:

1. **Додати API ендпоінт** для завантаження фото

   ```typescript
   POST / api / receipts / upload;
   ```

2. **OCR обробка** - розпізнавання тексту з чеків

   - Сума
   - Дата
   - Назва магазину
   - Категорії товарів

3. **Зберігання файлів** - використання cloud storage (AWS S3, CloudFlare, etc.)

4. **Оптимізація зображень** - стиснення перед відправкою

### Приклад майбутньої реалізації:

```typescript
const handleFormSubmit = async (data: CreateExpenseDto) => {
  // 1. Спочатку завантажуємо фото
  const uploadedReceipts = await uploadReceipts(receiptPhotos);

  // 2. Додаємо ID завантажених фото до даних
  const expenseData = {
    ...data,
    receiptIds: uploadedReceipts.map((r) => r.id),
  };

  // 3. Створюємо витрату з прив'язкою до чеків
  await createExpense(expenseData);
};
```

## Особливості використання

### На мобільних пристроях

- При натисканні "Камера" - відкривається камера пристрою
- При натисканні "Галерея" - відкривається галерея фото
- Підтримується множинний вибір (якщо дозволяє пристрій)

### На десктопі

- Обидві кнопки відкривають файловий менеджер
- Можна обрати множину файлів одночасно

### Обмеження

- Максимум 5 фото за замовчуванням (налаштовується)
- Кожне фото конвертується в Base64
- Великі файли можуть уповільнити роботу

## Стилізація

Компонент використовує Ionic компоненти та CSS змінні:

- `--ion-color-primary`
- `--ion-color-success`
- `--ion-color-medium`
- `--ion-color-light`

Можна кастомізувати через CSS змінні у `theme/variables.css`.

## Тестування

### На демо-сторінці (`/demo/receipt-upload`)

1. ✅ Завантажте декілька фото
2. ⏳ Спостерігайте за процесом обробки (жовті чіпи "Обробка...")
3. ✅ Перевірте успішно оброблені фото (зелені чіпи "Готово")
4. ❌ Перевірте фото з помилками (червоні чіпи "Помилка" + текст помилки)
5. 🔍 **Клікніть на будь-яке фото** - відкриється у повному розмірі з детальною інформацією
6. 📊 Подивіться статистику: скільки обробляється/успішно/з помилками
7. 🗑️ Видаліть фото з помилками
8. ♻️ Завантажте нові фото замість помилкових
9. 🚫 Спробуйте натиснути "Обробити чеки" при наявності помилкових фото

### У формах (витрати/ТО)

1. Додайте витрату або ТО
2. Завантажте фото чеків
3. 🔍 Клікніть на фото для перегляду у повному розмірі
4. Спробуйте зберегти форму:
   - Під час обробки фото - показує попередження ⏳
   - З помилковими фото - показує помилку і блокує збереження ❌
   - Тільки з успішними фото - зберігає форму ✅

### Перегляд фото у повному розмірі

- Клікніть на будь-яке фото в сітці
- Відкриється модальне вікно з фото у повному розмірі
- Показує назву файлу, розмір та статус обробки
- Закрити можна кліком на фон або кнопку закриття

## Troubleshooting

### Камера не відкривається

- Перевірте дозволи браузера на доступ до камери
- На iOS потрібен HTTPS для доступу до камери

### Фото не завантажуються

- Перевірте формат файлів (має бути зображення)
- Перевірте розмір файлів (великі файли можуть викликати помилки)

### Повільна робота

- Зменшіть `maxPhotos`
- Додайте стиснення зображень перед конвертацією у Base64

## Контакти

При виникненні питань або пропозицій щодо покращення компонента:

- Створіть issue у репозиторії
- Зв'яжіться з командою розробки
