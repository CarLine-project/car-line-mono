# Shared UI Components

Загальні UI компоненти, що використовуються в додатку.

## Компоненти

### 📤 ReceiptUpload

**Файл:** `receipt-upload.tsx`

Компонент для завантаження та обробки фото чеків з автоматичною валідацією.

#### Швидкий старт

```tsx
import { ReceiptUpload, ReceiptPhoto } from "../../shared/ui";

const [photos, setPhotos] = useState<ReceiptPhoto[]>([]);

<ReceiptUpload maxPhotos={5} onPhotosChange={setPhotos} disabled={false} />;
```

#### Особливості

- 📸 Камера та галерея
- ⚙️ Автоматична валідація (симуляція)
- ✅ Три стани: processing, success, error
- 🔍 Перегляд у повному розмірі
- 🚫 Захист від помилок у формах

#### Документація

- [Повна документація](../../../RECEIPT_UPLOAD.md)
- [Швидкий старт](../../../RECEIPT_UPLOAD_QUICK_START.md)
- [Приклади використання](./receipt-upload.example.tsx)
- [Тестування](../../../RECEIPT_UPLOAD_TESTING.md)

#### Демо

URL: `/demo/receipt-upload`

---

### 🔐 ProtectedRoute

**Файл:** `protected-route.tsx`

Компонент для захисту маршрутів від неавторизованих користувачів.

```tsx
<Route path="/protected">
  <ProtectedRoute>
    <ProtectedPage />
  </ProtectedRoute>
</Route>
```

---

### 📝 Input

**Файл:** `input.tsx`

Стилізований input компонент з підтримкою помилок.

```tsx
<Input
  label="Назва"
  placeholder="Введіть назву"
  error={errors.name?.message}
  {...register("name")}
/>
```

---

### 🔘 Button

**Файл:** `button.tsx`

Базовий button компонент.

```tsx
<Button onClick={handleClick}>Натисніть мене</Button>
```

---

## Експорт

Всі компоненти експортуються через `index.ts` для зручності:

```tsx
import {
  Input,
  Button,
  ProtectedRoute,
  ReceiptUpload,
  type ReceiptPhoto,
} from "../../shared/ui";
```

## Створення нових компонентів

1. Створіть файл `component-name.tsx`
2. Додайте експорт до `index.ts`
3. Створіть приклади використання (опціонально)
4. Оновіть цей README
