# Інструкція з наповнення бази даних тестовими даними

## Передумови

Перед запуском seeder переконайтеся, що:

1. ✅ База даних запущена (`npm run db:start`)
2. ✅ Користувач **btihovic@gmail.com** зареєстрований в системі

## Як зареєструвати користувача

Якщо користувач ще не зареєстрований, виконайте один з варіантів:

### Варіант 1: Через API (рекомендовано)

Запустіть бекенд:

```bash
cd car-line-back
npm run start:dev
```

Потім відправте POST запит на `http://localhost:3000/auth/register`:

```json
{
  "email": "btihovic@gmail.com",
  "password": "123454321",
  "name": "Bogdan Tihovic"
}
```

### Варіант 2: Через фронтенд додаток

1. Запустіть фронтенд: `cd car-line-front && npm start`
2. Відкрийте додаток і зареєструйтесь

## Запуск seeder

Після реєстрації користувача запустіть seeder:

```bash
cd car-line-back
npm run db:seed
```

## Що буде створено

Seeder створить для користувача **btihovic@gmail.com**:

### 🚗 2 машини:

1. **Toyota Camry 2018**
   - Початковий пробіг: 85,000 км
   - Статус: Активна

2. **Honda Civic 2020**
   - Початковий пробіг: 45,000 км
   - Статус: Неактивна

### 💰 20 витрат:

- 10 витрат для Toyota Camry (паливо, ремонт, ТО, страховка, мийка, парковка)
- 10 витрат для Honda Civic (аналогічні категорії)

### 🔧 10 записів обслуговування:

- 5 записів для Toyota Camry (заміна масла, колодок, ремонт підвіски, свічки, ТО)
- 5 записів для Honda Civic (заміна масла, фільтрів, дисків, ТО, балансування)

### 📊 10 записів пробігу:

- 5 записів для Toyota Camry (від 85,000 до 95,000 км)
- 5 записів для Honda Civic (від 45,000 до 52,000 км)

## Повторний запуск

Якщо ви хочете повторно наповнити базу:

1. Видаліть існуючі дані через API або базу даних
2. Або скиньте всю базу: `npm run db:reset`
3. Зареєструйте користувача знову
4. Запустіть seeder: `npm run db:seed`

## Зміна email користувача

Якщо потрібно створити дані для іншого користувача, відредагуйте файл:
`src/database/seed.ts` і змініть константу `TEST_USER_EMAIL`

## Можливі помилки

### ❌ Користувача не знайдено

```
❌ Користувача з email btihovic@gmail.com не знайдено!
```

**Рішення**: Спочатку зареєструйте користувача (див. вище)

### ⚠️ У користувача вже є машини

```
⚠️ У користувача вже є 2 машин(и). Пропускаю створення...
```

**Рішення**: Видаліть існуючі машини або скиньте базу даних

### ❌ Категорії витрат не знайдено

```
❌ Категорії витрат не знайдено!
```

**Рішення**: Seeder автоматично створює категорії. Якщо бачите цю помилку, перевірте підключення до БД.

## Перевірка результатів

Після успішного виконання ви побачите:

```
✅ Тестові дані для користувача успішно створено!

📈 Підсумок:
   - Машин: 2
   - Витрат: 20
   - Записів обслуговування: 10
   - Записів пробігу: 10
```

Тепер можете увійти в додаток з credentials:

- **Email**: btihovic@gmail.com
- **Password**: 123454321
